export const providerConstants = {
  PROVIDER_LOAD: '[Provider] Load',
  PROVIDER_LOAD_SUCCESS: '[Provider] Load success',
  PROVIDER_LOAD_FAILURE: '[Provider] Load failure',
  PROVIDER_RESET: '[Provider] Reset'
}
