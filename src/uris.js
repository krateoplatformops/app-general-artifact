const uris = {
  apiBase: window.runConfig.apiBaseUrl,
  auth: 'auth',
  config: 'config',
  user: 'user',
  logout: 'auth/logout',
  register: 'register',
  template: 'template',
  deployment: 'deployment',
  socket: window.runConfig.socketUrl,
  proxy: 'proxy',
  endpoint: 'endpoint',
  log: 'log',
  dashboard: 'dashboard'
}

export default uris
