const uris = {
  apiBase: window.runConfig.apiBaseUrl,
  auth: 'auth',
  config: 'config',
  user: 'user',
  logout: 'auth/logout',
  register: 'register',
  socket: window.runConfig.socketUrl
}

export default uris
