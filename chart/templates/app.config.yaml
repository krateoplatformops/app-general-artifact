apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  runtime-config.js: |
    window['runConfig'] = {
      apiBaseUrl: '{{ .Values.uris.api }}',
      socketUrl: '{{ .Values.uris.socket }}',
      title: '{{ .Values.title }}'
    }